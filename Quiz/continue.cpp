#include "continue.h"
#include "player.h"

void continueGame(Player& player, Difficulty difficulty) {
    // Implementation of what happens when the user chooses to continue
    // if newGame(player)=false, show player list (alternatively, enter name and check if it exists)
    // pick by number
    // ask difficulty (if not unlocked, cant pick) (e, n, h)
    // assign to player.difficulty
};
