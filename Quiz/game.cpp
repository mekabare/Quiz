#include "game.h"
#include "question.h"
#include "player.h"

void startGame(Difficulty difficulty) {
    // Implementation of the quiz game algorithm
    
    // bool repeat = false;
    // do while bool repeat=true
    // Question q //active instance
    // ifstream qfile;
    // string filename;
    // bool win = true;
    // int index; //(current question position)
    
    //switch (difficulty)
        //case EASY: filename = "easy_questions.txt";break;
        //case NORMAL: filename = "normal_questions.txt" ;break;
        //case HARD: filename = "hard_questions.txt"; break;

    //qfile.open(filename, ios::in); 
    //vector <Question> shuffledQuestions = shuffleQuestions(allQuestions) //reads from file, puts into vector and shuffles questions
    //vector <Question> t1Questions = Question::filterByTier(shuffledQuestions, 1);
    //vector <Question> t2Questions = Question::filterByTier(shuffledQuestions, 2);
    //vector <Question> t3Questions = Question::filterByTier(shuffledQuestions, 3);
    //vector <Question> t4Questions = Question::filterByTier(shuffledQuestions, 4);
    //vector <Question> t5Questions = Question::filterBtTier(shuffledQuestions, 5);

    // =================================== Quiz-Loop here ============================================
        //while (win)

            //flavor text
    
                //while (index < 4):
                // displayQuestion(index, t1Questions, q)
                // case 1: string prize = 50 EUR;
                // case 2: prize = 100 EUR;
                // case 3: prize = 200 EUR;
                
                //while (3 < index <7)
                // displayQuestion(index, t2Questions, q)
                // case 4: int prize = 300 EUR;
                // case 5: prize = 500 EUR;
                // case 6: prize = 1000 EUR;

                //while (6 < index < 10)
                // displayQuestion(index, t3Questions, q)
                // case 7: int prize = 2000 EUR;
                // case 8: prize = 4000 EUR;
                // case 9: prize = 8000 EUR;

                 //while (9 < index <13)
                // displayQuestion(index, t4Questions, q)
                // case 10: int prize = 16000 EUR;
                // case 11: prize = 32000 EUR;
                // case 12: prize = 64000 EUR;

                //while (12 < index <=15)
                // displayQuestion(index, t5Questions, q)
                // case 13: int prize = 125000 EUR;
                // case 14: prize = 500000 EUR;
                // case 15: prize = 1000000 EUR;


    // if (!win)
       // "oh too bad"
       //  player.score = prize
            // try again? (y/n)
            // switch(_getch())
            // case y: repeat=true
            // case n: arrowMenu();
     

}
